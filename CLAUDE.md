# Project Guidelines

## Requirements

`REQUIREMENTS.md` is the authoritative specification for this project. Whenever functionality is added, changed, or removed — in `generate_handbook.py`, `build_viewer.py`, `viewer.html`, or `sw.js` — update `REQUIREMENTS.md` to reflect the new state before or as part of the same commit.
